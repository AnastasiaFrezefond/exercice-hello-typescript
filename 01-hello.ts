// Hour 1 - TypeScript

const texte = "Hello World !";
console.log(texte);

// Tout ce qu'on a appris en JS on peut l'écrire dans un fichier.ts
// Ce fichier n'est pas compris par les navigateur web 
// Du coup il faut le transpiler en JS qui sera lui même compilé 
// Pour se faire, on va utiliser notre terminal tsc *.ts

